// Vitest stub for Next.js's "server-only" package (which only resolves
// inside Next's own bundler, not plain Node/Vitest module resolution).
// Aliased in vitest.config.ts so modules that import "server-only" can
// still be unit-tested directly.
export {};
